[package]
name = "narration-sync-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
narration-sync = { path = "../narration-sync" }
serde_json = "1"
wasm-bindgen = "0.2"
